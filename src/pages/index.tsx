import React from 'react';
import { SEO } from '../components/layout/seo';
import HomePage from './home';

export const Head = () => (
    <SEO title={'Home'} description={'UX/UI Designer'} pathname={'/'} />
);

export default HomePage;
